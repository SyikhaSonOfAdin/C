#include <stdio.h>
#define MAKS 50


int main()
{
    
}
int input( int big )
{
    int nilai[big] ;
    int loop1 ;
    for(loop1 = 0 ; loop1 <= big - 1 ; loop1 = loop1 + 1)
    {
        printf("Masukkan bilangan ke-%i : ", loop1 + 1) ;
        scanf("%i", &input2) ;
        maxSeeker[loop1] = input2 ;
        if( maxSeeker[loop1] > biggest )
        {
            biggest = maxSeeker[loop1] ;
        }
    }
int findmax( int nilai[] )
{

}